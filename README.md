# base-image-for-pipeline
This is a Dockerfile that builds the base image used in CICD pipeline.

## Base image:
alpine:latest

## Included tools:
* bash
* git
* curl
* yq (yaml parser)
* jq (json parser)
